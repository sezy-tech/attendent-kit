import { Text } from "react-native";

export default function Schedule(){

    return(
        <Text style={{
            alignItems : 'center',
            justifyContent : 'center'
        }}>
            schedule
        </Text>
    )
}